pipeline {
    agent any 
        
	    stages {
	         stage('build') {
		           steps {
			             echo 'Hello Praveen from git changed name of file'
				               }
					            }
						       }
						       }
